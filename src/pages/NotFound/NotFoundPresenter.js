import React, { Fragment } from "react";
import { Helmet } from "react-helmet";

/**
 * NotFoundPresenter
 */
const NotFoundPresenter = () => (
  <Fragment>
    <Helmet>
      <title>NotFoundPage : BoilerPlate</title>
    </Helmet>
    <div>
      <p>NotFoundPage 입니다.</p>
    </div>
  </Fragment>
);
export default NotFoundPresenter;
